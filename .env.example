# Port of this web UI (host port when using docker compose)
WEB_PORT=8025
# Where the SQLite history is stored (docker compose mounts ./storage)
STORAGE_DIR=storage
# Optional: passphrase for encrypting saved passwords (default: random key in storage/secret.key)
# ENCRYPTION_KEY=
